apiVersion: apps/v1
kind: Deployment
metadata:
  name: komunas-app
  namespace: linqra
  labels:
    app: komunas-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: komunas-app
  template:
    metadata:
      labels:
        app: komunas-app
    spec:
      containers:
        - name: komunas-app
          image: 945104357909.dkr.ecr.us-west-2.amazonaws.com/komunas-app:latest
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "ec2"
            - name: CLIENT_TRUST_STORE
              value: "/app/client-truststore.jks"
            - name: CLIENT_TRUST_STORE_PASSWORD
              value: "123456"
            - name: CLIENT_KEY_STORE
              value: "/app/client-keystore.jks"
            - name: CLIENT_KEY_STORE_PASSWORD
              value: "123456"
            - name: CLIENT_ALIAS_NAME
              value: "komunas-app-container"
            # Eureka config - use K8s service name
            - name: EUREKA_USERNAME
              valueFrom:
                secretKeyRef:
                  name: komunas-secrets
                  key: eureka-username
            - name: EUREKA_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: komunas-secrets
                  key: eureka-password
            - name: EUREKA_INSTANCE_URL
              value: "komunas-app"  # K8s service name, matches cert CN!
            - name: GATEWAY_SERVICE_URL
              value: "api-gateway-service"
            # MongoDB - from Kubernetes secret
            - name: MONGO_DB_URI
              valueFrom:
                secretKeyRef:
                  name: komunas-secrets
                  key: mongo-uri
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /r/komunas-app/health
              port: 8082
              scheme: HTTPS
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /r/komunas-app/health
              port: 8082
              scheme: HTTPS
            initialDelaySeconds: 60
            periodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: komunas-app
  namespace: linqra
spec:
  selector:
    app: komunas-app
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
  type: ClusterIP